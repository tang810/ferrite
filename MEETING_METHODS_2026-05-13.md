# 2026-05-13 组会录制中提到的方法整理

录制：唐雨梦的快速会议  
时间：2026-05-13 18:42:50  
主题：多层铁氧体/磁屏蔽结构的磁噪声仿真、评价与后续实验设计

> 本文不是逐字稿，而是把录制中提到的实验方法、仿真方法、评价指标和后续执行步骤转成可操作文字。

## 1. 总体研究路线

会议中明确的总体思路是：

```text
Maxwell 批量仿真不同磁屏蔽结构
-> 得到大量磁噪声/积分值数据
-> 整理成训练集
-> 用 AI/机器学习寻找结构参数与磁噪声之间的规律
-> 回到 Maxwell 或实验中验证最优结构
```

前期不应直接上 AI。当前最重要的是把 Maxwell 数据集做干净，尤其是结构参数、噪声指标、屏蔽指标要一一对应。

## 2. Maxwell 批量计算方法

录制开头提到，需要用 Maxwell 软件计算多种构型、厚度和结构下的磁噪声，形成大约 1000 组数据。

建议数据结构为：

```text
N, a, g, T, geometry_id, IntH2_total, IntH2_L1, IntH2_L2, ..., B_avg, SF
```

其中：

- `N`：层数
- `a`：单层铁氧体厚度
- `g`：层间空气间隙
- `T`：总径向厚度
- `IntH2_total`：总磁场能量相关积分
- `IntH2_Li`：第 i 层单独积分
- `B_avg`：1-30 Hz 平均磁噪声
- `SF`：屏蔽系数

方法重点：先用 Maxwell 自动化脚本批量跑数据，再把结果交给 AI/机器学习，而不是手工一点点改模型。

## 3. 参数化建模方法

会议中反复讨论多层壳/多层铁氧体结构。参数化关系应采用：

```text
T = N*a + (N-1)*g
```

如果固定总厚度 `T` 和间隙 `g`，则：

```text
a = [T - (N-1)*g] / N
```

这条公式是下一步实验的核心。

示例：

```text
T = 10 mm
g = 0.2 mm
N = 4
a = [10 - 3*0.2] / 4 = 2.35 mm
```

## 4. 固定总厚度对比层数的方法

会议的核心问题之一是：

```text
在总厚度固定时，增加层数是否真的能降低磁噪声？
```

因此后续应该先做固定总厚度对比，而不是继续简单加厚。

推荐第一组：

```text
T = 10 mm
g = 0.2 mm
N = 1, 2, 3, 4, 5, 6, 7
```

对应：

```text
N=1, a=10.0 mm
N=2, a=4.9 mm
N=3, a=3.2 mm
N=4, a=2.35 mm
N=5, a=1.84 mm
N=6, a=1.5 mm
N=7, a=1.257142857 mm
```

推荐第二组：

```text
T = 10 mm
g = 0.5 mm
N = 1, 2, 3, 4, 5, 6, 7
```

这组用于和前期 `g=0.5 mm` 的旧结果对照。

## 5. 薄层可实现性方法

会议中提到实际结构可能受到产品厚度、加工厚度、间隙大小限制，例如：

```text
a = 0.3 mm
g = 0.2 mm
```

这不是最优性扫描，而是“实验上能不能做”的可实现性验证。

推荐扫描：

```text
a = 0.3 mm
g = 0.2 mm
N = 4, 5, 6, 7, 8
```

对应总厚度：

```text
N=4, T=1.8 mm
N=5, T=2.3 mm
N=6, T=2.8 mm
N=7, T=3.3 mm
N=8, T=3.8 mm
```

这个方法用于回答：

```text
如果单层材料只能做很薄，靠增加层数是否还能改善磁噪声或屏蔽效果？
```

## 6. 最内层积分值方法

会议里明确讨论了一个问题：层数增加后，总噪声可能变大，因为每一层都贡献噪声。因此不能只看总值，需要导出每一层的噪声贡献。

后续应导出：

```text
IntH2_L1
IntH2_L2
IntH2_L3
...
IntH2_total
```

重点先看：

```text
IntH2_L1
```

也就是最内层积分值。

原因：

- 最内层离被保护区域最近
- 最内层对中心区域噪声贡献最直接
- 如果总噪声变大，可以判断到底是哪一层贡献大

## 7. Maxwell Field Calculator 操作方法

每层铁氧体必须单独保留为独立实体，不能 Unite 成一个整体。

命名建议：

```text
ferrite_L1
ferrite_L2
ferrite_L3
...
```

你当前 4 层工程中实际对应关系是：

```text
Cylinder2 = 第 1 层铁氧体
Cylinder4 = 第 2 层铁氧体
Cylinder6 = 第 3 层铁氧体
Cylinder8 = 第 4 层铁氧体
```

Field Calculator 中每层操作：

```text
H
-> Mag
-> Square
-> 选择某一层铁氧体体积
-> Integrate
-> 保存为 IntH2_Li
```

总积分：

```text
IntH2_total = IntH2_L1 + IntH2_L2 + ... + IntH2_LN
```

注意：你现有 4 层工程里第 4 层表达式叫 `InH2_s4`，少了一个 `t`，但 `IntH2_total` 已经引用它。后续建议统一改成 `IntH2_L4`。

## 8. 不删除原结果的方法

会议中提到不要轻易删除原来的计算结果，防止跑丢。

执行原则：

```text
先 Save As / 另存工程
再新增 calculation
再重新跑
```

不要直接覆盖：

```text
Project100_1ceng.aedt
Project100_2ceng.aedt
Project100_3ceng.aedt
Project100_4ceng.aedt
```

建议使用：

```text
round2_working/Project100_1ceng_next.aedt
round2_working/Project100_2ceng_next.aedt
round2_working/Project100_3ceng_next.aedt
round2_working/Project100_4ceng_next.aedt
```

## 9. 平均磁噪声与积分值评价方法

会议摘要中提到两类评价：

```text
磁噪声平均值
磁导数/磁场相关积分值
```

当前项目里已经采用：

```text
IntH2_total = integral(H^2 dV)
```

并通过 MATLAB 转换成：

```text
B_avg_1_30_fT
```

后续建议评价指标分为三层：

```text
第一层：IntH2_L1，最内层贡献
第二层：IntH2_total，总噪声来源
第三层：B_avg_1_30_fT，最终噪声指标
```

## 10. 屏蔽系数评价方法

会议后段提到后续要关注屏蔽系数。

方法：

```text
无屏蔽体时中心磁场 = B0_T
有屏蔽体时中心磁场 = Bcenter_T
```

计算：

```text
SF = abs(B0_T) / abs(Bcenter_T)
ResidualRatio = abs(Bcenter_T) / abs(B0_T)
```

判断：

- `SF` 越大，屏蔽效果越好
- `ResidualRatio` 越小，中心残余磁场越小

重要：不能只追求低磁噪声，还要同时看屏蔽效果。

## 11. 多目标优化方法

会议中的真实问题不是单目标最小噪声，而是多目标折中：

```text
低磁噪声
高屏蔽系数
低材料体积
结构可加工
```

推荐目标函数：

```text
Score = w1 * normalize(B_avg)
      + w2 * normalize(1/SF)
      + w3 * normalize(MaterialVolume)
```

初始权重可用：

```text
w1 = 0.5  噪声
w2 = 0.3  屏蔽
w3 = 0.2  材料体积
```

如果当前阶段还没有 `SF`，先用：

```text
B_avg + MaterialVolume
```

做临时评分。

## 12. 结构尺寸与实验实现方法

会议中提到的结构量级包括：

```text
内径/内方约 100 mm
高度约 200 mm
```

也讨论了多片拼装：

```text
每层约 8 片
7 层约 56 片
```

后续如果从仿真走向实物，需要考虑：

- 每片铁氧体尺寸
- 层间间隙如何保证
- 非磁性支撑结构
- 拼接缝隙
- 实际厚度误差
- 安装后是否引入额外磁污染

## 13. 材料参数测试方法

会议中提到未来需要测剩磁和材料参数。

建议后续补充：

```text
B-H 曲线
剩磁 Br
低频复磁导率 μ'(f), μ''(f)
1-30 Hz 频段材料参数
温度条件
```

原因：

当前 Maxwell/MATLAB 计算的绝对噪声值依赖材料损耗参数。如果材料参数不准，绝对数值会有偏差，但不同结构之间的相对趋势仍有参考价值。

## 14. AI 数据集方法

会议中提到的 AI 方法不是直接替代 Maxwell，而是：

```text
Maxwell 生成训练数据
AI 学习结构参数到噪声/屏蔽指标的映射
AI 给出候选最优结构
Maxwell 再反验证
```

训练输入：

```text
N, a, g, T, eta, gamma, MaterialVolume, geometry_type
```

其中：

```text
eta = N*a / T
gamma = g / a
```

训练输出：

```text
B_avg_1_30_fT
IntH2_total
IntH2_L1
SF
Score
```

推荐先做 100-200 组干净数据验证流程，再扩大到 1000 组。

## 15. 会议方法转成下一步执行清单

第一步：

```text
另存 1-4 层工程，不覆盖原结果
```

第二步：

```text
补充分层 calculation：
IntH2_L1, IntH2_L2, ...
```

第三步：

```text
跑 fixedT10_g02：
T=10mm, g=0.2mm, N=1..7
```

第四步：

```text
导出每个点：
IntH2_L1, IntH2_total, B_avg, B0_T, Bcenter_T
```

第五步：

```text
MATLAB 后处理：
N vs B_avg
N vs IntH2_L1
N vs SF
每层 Qratio_Li
```

第六步：

```text
判断增加层数是否有效
```

判断逻辑：

```text
若 N 增加，B_avg 降低且 SF 提高：
多层结构有效。

若 N 增加，B_avg 升高但 SF 提高：
多层存在屏蔽收益，但有噪声代价，需要多目标优化。

若 N 增加，B_avg 升高且 SF 不提高：
当前静态模型下多层结构不划算，需要调整结构或考虑动态/材料实验。
```

## 16. 最重要的会议结论

这次会议真正要求你做的不是“继续随便扫参数”，而是：

```text
把 Maxwell 仿真从总噪声评价，升级成分层噪声 + 屏蔽系数 + 固定总厚度层数对比。
```

也就是要回答：

```text
增加层数到底降低了什么？
增加层数是否只是增加了噪声源？
最内层贡献是否变小？
屏蔽系数是否变大？
在实际可加工厚度下，多层结构是否值得做？
```

