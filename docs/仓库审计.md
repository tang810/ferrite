# 仓库审计

记录当前 ferrite 仓库的文件状态，区分 current manufacturable thin-ferrite 主实验、legacy thick-shell 历史数据、可直接用于论文的材料，以及仍缺失的数据。

日期：2026-05-27
分支：`experiment-completion-pipeline`

范围：多层圆柱薄铁氧体壳的有限元设计与评估框架。不完整的 AEDT 或 CSV 导出不算论文结果。

## 当前 AEDT 工程

### 可制造薄铁氧体候选

这些是论文当前方向最相关的 AEDT 工程，位于 `round2_working/manufacturable_thin/`。

C1_N1_t008_B0/t008_shield, C1_N1_t020_B0/t020_shield, C1_N1_t040_B0/t040_shield, C1_N1_t060_B0/t060_shield — 单层系列，B0 是无屏蔽参考，shield 是屏蔽连续壳。C2_N2_t020_g010_B0/shield (N=2), C2_N3_t020_g010_B0/shield (N=3), C2_N4_t015_g008_B0/shield (N=4) — 多层系列。全部为 priority-1 候选，导出已验证。

### 历史工程

以下工程是历史背景，不是可制造薄铁氧体主矩阵：

根目录的 Project100_1ceng/2ceng/3ceng/4ceng 及 *bak* 备份、Project100_4ceng_fixedT10_g02_ext_ferrite、round2_working/Project100_*、Cubic ferrite shielding(1)、Project1-5、halbach_catapult。

这些文件不要删。如果以后整理，移到 `aedt/legacy/` 并在 README 注明。

## 当前 CSV 结果

### 可制造薄铁氧体文件

`analysis_ready/manufacturable_thin_ferrite_sim_matrix.csv` 和 `next_round_design_points.csv` 是矩阵定义，规范化后可用。`manufacturable_thin_center_fields_template.csv` 是导出模板。`manufacturable_thin_center_fields_exported.csv` 是部分导出，不够做论文最终结果。`manufacturable_thin_evaluated.csv` 是历史评估输出，只做流水线冒烟测试。`manufacturable_thin_dataset.csv`、`manufacturable_thin_metrics.csv`、`layerwise_intH2_validation.csv` 是生成的，论文使用取决于导出是否完整。

`data/raw/center_field_raw.csv` 和 `data/raw/intH2_layerwise_raw.csv` 是当前规范导出路径，7 个 transverse 候选已填入。`data/processed/metrics_table.csv` 是规范指标表。`data/validation/layerwise_intH2_validation.csv` 和 `data/validation/B0_reference_validation.csv` 是规范验证表。

### 历史厚壳/探索性文件

这些是 legacy，别混进可制造薄铁氧体主结果：all_results_clean*.csv, all_results_layerwise.csv, agmatrix_clean.csv, asweep_clean.csv, Tcompare_clean.csv, formal_sf_fixedT10_g02_*, SF_4ceng_fixedT10_g02_*, matlab_B_results/*.csv, 根目录的 result_*, P_*, 100_*.csv, 2.csv, 3.csv, 3ce.csv, 4ceng_g.csv。

## 脚本

现有 AEDT 脚本大部分在 `round2_working/`，为探索性运行写的，可参考但不是可复现的论文流水线。包括 run_manufacturable_thin_continuous.py, export_manufacturable_thin_results.py, export_Bcenter_*.py 等。`analysis_ready/` 下有一些后处理脚本（evaluate_manufacturable_thin_results.py, compute_SF_from_B0_Bcenter.py, merge_*），用了旧命名或混了 legacy 概念。

新流水线放在 `scripts/aedt_export/`（rebuild_shields_tangentialH.py 等）、`scripts/postprocess/`、`scripts/plotting/`。

## 论文文件

当前草稿：`多层圆柱壳磁屏蔽论文/paper_intro_methods_draft.md`。审计时未找到 paper_intro_methods_draft(3).md。results skeleton 待生成到 `paper/results_skeleton.md`。

## 可直接用于论文

实验定义和计划矩阵结构、AEDT 工程文件清单、导出场命名约定、后处理流水线定义、手稿 Methods/Results 骨架（明确标注了缺失数据为 pending）。当前部分 CSV 导出不够做最终数值结论。

## 已完成数据

以下数据已通过规范流水线验证：

- B0_reference_x：x 向切向 H 无屏蔽参考。源文件 `aedt/reference/B0_reference_x.aedt`，导出 `data/raw/B0_reference_x.csv`，验证 `data/validation/B0_reference_validation.csv` 报告 passed。B0_Bx_T = 1.257e-06 T，方向比 > 1e7。
- 单层横向屏蔽中心场 + IntH2 导出（C1_N1_t008_x 到 t060_x）。全部 metric_status=processed, SFx>1, IntH2 分层验证通过。
- 多层横向屏蔽中心场 + IntH2 导出（C2_N2_t020_g010_x, C2_N3_t020_g010_x, C2_N4_t015_g008_x）。全部 metric_status=processed, SFx>1, 分层 IntH2 验证通过。

## 缺失数据

- 已验证的 B0_reference_z 导出（含 B0_Bx/By/Bz/Mag_T）
- z 向、分段 (S*)、灵敏度 (MU_*)、网格 (MESH_*) 的屏蔽中心场导出
- z 向、分段、灵敏度 case 的分层 IntH2_Li 导出
- 选定 z 向场的轴向延伸
- 分段 aligned/staggered 壳修正
- 网格和边界域收敛研究
- 代表性候选的磁导率灵敏度扫描

## 整理原则

不删原始文件。整理只做增量：新规范输入放 `data/raw/`、`data/processed/`、`data/validation/`，新脚本放 `scripts/`，论文图表放 `figures/`。Legacy 和探索性文件原地保留，等单独一次归档 commit 再移走并写文档。
